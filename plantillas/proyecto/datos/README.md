# Datos

Esta carpeta **no va en git**: los datos de un cliente suelen ser grandes y
confidenciales. Sólo este archivo se commitea.

- **Dónde viven de verdad:** <la carpeta compartida, el servidor, la base del
  cliente> — y quién da acceso.
- **Cómo se trae una copia aquí:** <el comando o los pasos>.
- **Lo que sí entra en git:** sólo lo derivado y anonimizado que un análisis
  necesita para reproducirse, y en otra carpeta (`docs/` o `scripts/`), no
  aquí.
